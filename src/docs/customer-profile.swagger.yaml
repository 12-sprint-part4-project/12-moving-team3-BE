components:
  schemas:
    CustomerProfileResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required:
            - profileId
            - userId
            - name
            - email
            - phoneNumber
            - profileImageUrl
            - service
            - region
            - updatedAt
          properties:
            profileId:
              type: integer
              example: 15
            userId:
              type: string
              format: uuid
              example: '3fa85f64-5717-4562-b3fc-2c963f66afa6'
            name:
              type: string
              example: "???"
            email:
              type: string
              format: email
              example: codeit@email.com
            phoneNumber:
              type: string
              nullable: true
              example: '01012345678'
            profileImageUrl:
              type: string
              nullable: true
              description: "??? Presigned GET URL (?? ?? 1??). ??? ??? null"
              example: null
            service:
              type: array
              items:
                type: string
                enum: [SMALL, HOME, OFFICE]
              example: [SMALL]
            region:
              type: string
              nullable: true
              enum:
                - SEOUL
                - GYEONGGI
                - INCHEON
                - GANGWON
                - CHUNGBUK
                - CHUNGNAM
                - SEJONG
                - DAEJEON
                - JEONBUK
                - GWANGJU_JEONNAM
                - GYEONGBUK
                - DAEGU
                - ULSAN
                - GYEONGNAM
                - BUSAN
                - JEJU
              example: SEOUL
            updatedAt:
              type: string
              format: date-time
              example: '2026-07-28T08:20:00.000Z'

    CustomerProfileMeResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required:
            - profileId
            - userId
            - name
            - email
            - phoneNumber
            - profileImageUrl
            - service
            - region
            - createdAt
            - updatedAt
          properties:
            profileId:
              type: integer
              example: 15
            userId:
              type: string
              format: uuid
              example: '3fa85f64-5717-4562-b3fc-2c963f66afa6'
            name:
              type: string
              example: "???"
            email:
              type: string
              format: email
              example: codeit@email.com
            phoneNumber:
              type: string
              nullable: true
              example: '01012345678'
            profileImageUrl:
              type: string
              nullable: true
              description: "??? Presigned GET URL (?? ?? 1??). ??? ??? null"
              example: null
            service:
              type: array
              items:
                type: string
                enum: [SMALL, HOME, OFFICE]
              example: [SMALL]
            region:
              type: string
              nullable: true
              enum:
                - SEOUL
                - GYEONGGI
                - INCHEON
                - GANGWON
                - CHUNGBUK
                - CHUNGNAM
                - SEJONG
                - DAEJEON
                - JEONBUK
                - GWANGJU_JEONNAM
                - GYEONGBUK
                - DAEGU
                - ULSAN
                - GYEONGNAM
                - BUSAN
                - JEJU
              example: SEOUL
            createdAt:
              type: string
              format: date-time
              example: '2026-07-20T13:30:00.000Z'
            updatedAt:
              type: string
              format: date-time
              example: '2026-07-21T10:00:00.000Z'

paths:
  /api/users/customers/profile:
    get:
      tags: [Customers]
      summary: "?? ?? ?? ??? ??"
      description: |
        ???? ?? ??(CUSTOMER)? ??? ???? ?????.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: "??? ?? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileMeResponse'
        '401':
          description: "??? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: "?? ??(CUSTOMER)? ?? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: "??? ??? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      tags: [Customers]
      summary: "?? ?? ??? ??/??"
      description: |
        ?? ??(CUSTOMER)? ??? ??? ????? ?????.
        - Content-Type: application/json
        - ??(?? service ???): region, service ??. ???? ?? ??? ?????.
        - ??(?? ???): ??? ??? ?? ?????. ?? ??? ??? NO_CHANGE.
        - ???? ?? ? newPassword? ?? currentPassword, newPasswordConfirm? ?????.
        - s3Key? ??????. GET /api/presigned-upload-url? ???? ? ?? key? ?????.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: "??? ?? (2~20?)"
                phoneNumber:
                  type: string
                  description: "??? ???? (?? 11??)"
                currentPassword:
                  type: string
                  description: "???? ?? ? ?? ???? (??? ??)"
                newPassword:
                  type: string
                  description: "??? ? ???? (8~20?, ??/??/????)"
                newPasswordConfirm:
                  type: string
                  description: "? ???? ?? (newPassword ?? ? ??)"
                region:
                  type: string
                  description: "?? ?? (?? ? ??)"
                  enum:
                    - SEOUL
                    - GYEONGGI
                    - INCHEON
                    - GANGWON
                    - CHUNGBUK
                    - CHUNGNAM
                    - SEJONG
                    - DAEJEON
                    - JEONBUK
                    - GWANGJU_JEONNAM
                    - GYEONGBUK
                    - DAEGU
                    - ULSAN
                    - GYEONGNAM
                    - BUSAN
                    - JEJU
                service:
                  type: array
                  description: "?? ??? ?? (?? ? ??, ?? 1?)"
                  minItems: 1
                  items:
                    type: string
                    enum: [SMALL, HOME, OFFICE]
                s3Key:
                  type: string
                  description: "Presigned ??? ? ?? S3 key"
                  example: "profile-images/1785389998366_conan.jpg"
            example:
              name: "???"
              phoneNumber: '01012345678'
              currentPassword: OldPass123!
              newPassword: NewPass123!
              newPasswordConfirm: NewPass123!
              region: SEOUL
              service: [SMALL]
              s3Key: profile-images/1785389998366_conan.jpg
      responses:
        '200':
          description: "??? ??/?? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          description: "?? ?? ?? (NO_CHANGE, CURRENT_PASSWORD_REQUIRED, NEW_PASSWORD_MISMATCH ?)"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: "??? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: "?? ??(CUSTOMER)? ?? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: "?? ?? ??? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: "???? ??"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'